#include <limits.h>
#include <iostream>
#include "PhoneBook.hpp"

int main() {
	PhoneBook	phonebook;
	std::string	command;

	while (true)
	{
		std::cout << "Enter command (ADD/SEARCH/EXIT): ";
		std::cin >> command;
		
		}
	}
	return 0;
}
